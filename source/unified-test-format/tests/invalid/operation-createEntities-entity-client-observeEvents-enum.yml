description: "entity-client-observeEvents-enum"

schemaVersion: "1.9"

tests:
  - description: "foo"
    operations:
      - name: createEntities
        object: testRunner
        arguments:
          entities:
            - client:
                id: &client0 "client0"
                observeEvents: ["foo"]
